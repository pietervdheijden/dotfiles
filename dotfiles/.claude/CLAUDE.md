# PR Guidelines

When creating pull requests, do not include a "Test plan" section in the PR body.

# Git Guidelines

Never force push (`git push --force`, `git push -f`, `git push --force-with-lease`, or any other force variant).
